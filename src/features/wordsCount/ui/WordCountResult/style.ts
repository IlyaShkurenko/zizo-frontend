import styled from 'styled-components';
import { BaseContainer } from '@/shared/ui/styles/BaseContainer';

export const ResultsContainer = styled(BaseContainer)`
  background-color: #ffff;
  overflow-y: auto;
`;
